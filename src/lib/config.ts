// Fonctionnalités IA (insights dashboard + diagnostic incident) : masquées
// pour le moment côté UI. Les composants et routes API restent en place —
// repasser ce flag à true suffit pour tout réactiver.
export const AI_FEATURES_ENABLED = false;
